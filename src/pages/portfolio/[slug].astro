---
import PortfolioLayout from '../../layouts/PortfolioLayout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const projects = await getCollection('portfolio');
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await project.render();
const { title, description, type, technologies, image, imageBackgroundColor, imageFit, icon, iconBackgroundColor, linkGithub, linkWebsite, screenshotsAvailable, screenshotsPath, screenshotsFiles } = project.data;
const ogImage = `/og/portfolio/${project.slug}.jpg`;
---

<PortfolioLayout
  title={title}
  description={description}
  type={type}
  technologies={technologies}
  image={image}
  imageBackgroundColor={imageBackgroundColor}
  imageFit={imageFit}
  icon={icon}
  iconBackgroundColor={iconBackgroundColor}
  linkGithub={linkGithub}
  linkWebsite={linkWebsite}
  ogImage={ogImage}
  screenshotsAvailable={screenshotsAvailable}
  screenshotsPath={screenshotsPath}
  screenshotsFiles={screenshotsFiles}
>
  <Content />
</PortfolioLayout>
